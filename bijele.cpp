#include <bits/stdc++.h>
using namespace std;
int main()
{
    int q, ki, r, b, k, p;
    cin >> q >> ki >> r >> b >> k >> p;
    cout << (1 - q) << " " << (1 - ki) << " " << (2 - r) << " " << (2 - b) << " " << (2 - k) << " " << (8 - p);
}